
discord:
  botkey: "botkey"
  channel: "channel"
  qlength: 64
  waitms: "500ms"

ironrealms:
  apiurl: "https://api.lusternia.com"

database:
  connection: "user=ada password=ada dbname=ada-bot sslmode=disable"

settingsfile: settings.yaml
